# Documentation Archive: 2026-06 Reset

This directory preserves the pre-reset planning documents as historical reference.

Purpose:

- retain prior architecture thinking, tradeoffs, and mistakes
- avoid mixing old contracts with the rewritten planning set
- provide a reference base for future postmortem review

Usage rule:

- files here are historical artifacts
- active cross-module planning now belongs under the top-level `docs/` directory
- do not treat archived contracts as current source of truth
