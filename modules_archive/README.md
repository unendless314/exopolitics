# Module Archive

This directory contains archived module trees that are no longer the active implementation workspace.

Usage rules:

- treat contents here as historical reference only
- do not treat archived module docs or code as current implementation contracts
- active module work restarts under `modules/`

Current archive set:

- `2026-06-reset/`: pre-reset `ingest` and `classify` module snapshot
